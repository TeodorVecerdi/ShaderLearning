#pragma kernel CSMain

RWTexture2D<float4> Result;
float Resolution;

[numthreads(8,8,1)]
void CSMain (uint3 id : SV_DispatchThreadID){
    Result[id.xy] = float4(id.x / Resolution, id.y / Resolution, 1.0, 0.0);
}
